PVE自动测试软件

PVE自动测试软件一共分为4大功能模块
1.远程INCA功能
2.远程故障注入仪功能
3.故障码模式自动测试
4.脚本自动测试

故障码测试模式

通过OBD/UDS协议读取 通过INCA读取 通过故障注入设备读取

从总体测试配置中获取

从P001故障配置中获取

远程控制INCA标定

标定变量从配置文件中读取

手动控制故障注入仪

手动控制故障注入设备可以调用故障注入设备
的控制接口直接控制故障注入仪

另外配有延迟，通讯等简单接口，可以完成简
单的非闭环测试。

远程控制INCA

通过上位机，用户可以远程控制INCA（打开 关闭 读取变量列表 读取标量 改变变量）。

Phython 自动脚本运行

Phython 脚本模块可以运行客户自己编译的Phython脚本，我们提供有封装好的控制故障
注入仪、INCA控制（读取、写入变量）、UDS/OBD故障读取 的Phython接口供用户调用，
方便客户实现自动化闭环测试。

PVE自动测试软件

PVE自动测试软件一共分为4大功能模块
1.远程INCA功能
2.远程故障注入仪功能
3.故障码模式自动测试
4.脚本自动测试

故障码测试模式

通过OBD/UDS协议读取 通过INCA读取 通过故障注入设备读取

从总体测试配置中获取

从P001故障配置中获取

远程控制INCA标定

标定变量从配置文件中读取

手动控制故障注入仪

手动控制故障注入设备可以调用故障注入设备
的控制接口直接控制故障注入仪

另外配有延迟，通讯等简单接口，可以完成简
单的非闭环测试。

远程控制INCA

通过上位机，用户可以远程控制INCA（打开 关闭 读取变量列表 读取标量 改变变量）。

Phython 自动脚本运行

Phython 脚本模块可以运行客户自己编译的Phython脚本，我们提供有封装好的控制故障
注入仪、INCA控制（读取、写入变量）、UDS/OBD故障读取 的Phython接口供用户调用，
方便客户实现自动化闭环测试。

PVE自动测试软件

PVE自动测试软件一共分为4大功能模块
1.远程INCA功能
2.远程故障注入仪功能
3.故障码模式自动测试
4.脚本自动测试

故障码测试模式

通过OBD/UDS协议读取 通过INCA读取 通过故障注入设备读取

从总体测试配置中获取

从P001故障配置中获取

远程控制INCA标定

标定变量从配置文件中读取

手动控制故障注入仪

手动控制故障注入设备可以调用故障注入设备
的控制接口直接控制故障注入仪

另外配有延迟，通讯等简单接口，可以完成简
单的非闭环测试。

远程控制INCA

通过上位机，用户可以远程控制INCA（打开 关闭 读取变量列表 读取标量 改变变量）。

Phython 自动脚本运行

Phython 脚本模块可以运行客户自己编译的Phython脚本，我们提供有封装好的控制故障
注入仪、INCA控制（读取、写入变量）、UDS/OBD故障读取 的Phython接口供用户调用，
方便客户实现自动化闭环测试。

